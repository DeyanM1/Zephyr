# Python

This variable can't be changed.


* Import Python library:

```

__ ? LIB:./lib/python.py;

```

* Define Python:

```

python # python:; § No additional arguments needed

```


* Run python command

```

python ? run:<* pythonCommand>;

```

**pythonCommand is written in this format:**

Always use double quotes instead of single ones.

to use a variable value don't use ()

```

(print("Hello World"))

or

'variableWithCommandAsValue'
```

