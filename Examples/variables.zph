string # PT:Hallo;
number # INT:55;
decimal # FLOAT:3.14;


string ? push:;

number # CT:PT;
number ? push:;

decimal # CT:PT;
decimal ? push:;

number # CT:INT;
number ? w:++;


number # CT:PT;
number ? push:;

__ ? dumpVars:;